build/
src/*.json
src/*.npz
src/tracker.html
__pycache__/
*.pyc
.DS_Store

# Generated every run by src/build.py and deployed straight from the runner, so the
# committed copy was only ever a stale duplicate — and a local build would silently
# overwrite it with whatever data happened to be in build/data.json.
public/index.html
